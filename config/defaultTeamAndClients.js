/**
 * Fallback when no real team members or clients exist yet.
 * Use empty arrays so UI shows no data until the admin (or team member, if allowed)
 * adds real team members and clients.
 */

export const DEFAULT_TEAM_MEMBERS = [];

export const DEFAULT_CLIENTS = [];
