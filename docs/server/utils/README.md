# Server Utils

Server-side utility functions and helpers.

These utilities run only on the server (Node.js environment) and cannot be imported in client-side code.

## Usage

```javascript
// In API routes or server-side code
import { someUtil } from '@/server/utils/someUtil';
```
